[project]
name = "parabolic"
version = "1.0.0"
authors = [{ name = "crivero10" }]
readme = "README.md"
requires-python = ">=3.14"

dependencies = [
    "pandas",
    "numpy"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

